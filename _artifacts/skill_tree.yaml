# _artifacts/skill_tree.yaml
library:
  name: "@ntatoud/styra"
  version: "0.1.0"
  repository: "https://github.com/ntatoud/styra"
  description: >
    Type-safe class variance builder — a maintained, dependency-free CVA
    replacement. Produces component class strings from variant maps with full
    TypeScript inference.
generated_from:
  domain_map: "_artifacts/domain_map.yaml"
  skill_spec: "_artifacts/skill_spec.md"
generated_at: "2026-04-12"

# No router skill — 5 skills, intent CLI list is sufficient for discovery.
# No core overview — flat structure, each skill is standalone.
# No framework skills — library is framework-agnostic.
# Skills are co-located with packages/styra (monorepo).

skills:
  - name: styra-getting-started
    slug: styra-getting-started
    type: lifecycle
    domain: building-variants
    path: skills/styra-getting-started/SKILL.md
    package: packages/styra
    description: >
      First install through first working component variant with @ntatoud/styra.
      Covers styra(base).variants().defaults() builder chain, class/className override
      props, createStyra factory with tailwind-merge, cn utility, and VariantProps type helper.
    requires: []
    sources:
      - "ntatoud/styra:packages/styra/src/index.ts"
      - "ntatoud/styra:packages/styra/README.md"

  - name: styra-define-variants
    slug: styra-define-variants
    type: core
    domain: building-variants
    path: skills/styra-define-variants/SKILL.md
    package: packages/styra
    description: >
      Defining variant maps, boolean shorthand variants, and default values
      with @ntatoud/styra. Covers .variants(), boolean prop shorthand, .defaults(),
      VariantProps<T> type extraction, and clsx-like class/className props.
    requires:
      - styra-getting-started
    sources:
      - "ntatoud/styra:packages/styra/src/types.ts"
      - "ntatoud/styra:packages/styra/src/index.ts"

  - name: styra-compound-variants
    slug: styra-compound-variants
    type: core
    domain: compound-variants
    path: skills/styra-compound-variants/SKILL.md
    package: packages/styra
    description: >
      Writing compound variant rules with @ntatoud/styra — applying classes when
      multiple variants match simultaneously. Covers .compound([]), exact-match rules,
      and negation using the { not: value } syntax.
    requires:
      - styra-define-variants
    sources:
      - "ntatoud/styra:packages/styra/src/index.ts"
      - "ntatoud/styra:packages/styra/src/types.ts"

  - name: styra-migrate-from-cva
    slug: styra-migrate-from-cva
    type: lifecycle
    domain: migration
    path: skills/styra-migrate-from-cva/SKILL.md
    package: packages/styra
    description: >
      Drop-in migration from class-variance-authority (CVA) to @ntatoud/styra.
      API mapping: cva() config object → builder chain, defaultVariants → .defaults(),
      compoundVariants → .compound(), cx → cn. Covers new capabilities absent in CVA.
    requires: []
    sources:
      - "ntatoud/styra:packages/styra/src/index.ts"
      - "ntatoud/styra:packages/styra/README.md"

  - name: styra-shadcn-integration
    slug: styra-shadcn-integration
    type: composition
    domain: integration
    path: skills/styra-shadcn-integration/SKILL.md
    package: packages/styra
    description: >
      Replacing CVA with @ntatoud/styra in a shadcn/ui project. Covers createStyra
      factory with twMerge, shared lib/styra.ts pattern, replacing cva and cn imports,
      and eliminating the clsx dependency.
    requires:
      - styra-getting-started
    sources:
      - "ntatoud/styra:packages/styra/src/index.ts"
      - "ntatoud/styra:packages/styra/README.md"
    subsystems:
      - tailwind-merge
      - shadcn-ui
